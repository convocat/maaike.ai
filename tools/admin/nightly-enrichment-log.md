# Nightly enrichment log

Date-stamped log of the `nightly-inbox-enrichment` scheduled task. Newest entries at the bottom.

Each run appends one block. Format:

```
## YYYY-MM-DD
- enriched: slug-1, slug-2 (N total)
- skipped: slug-3 (reason), slug-4 (reason)
```

Reasons: `pdf-skipped`, `webfetch-failed`, `validation-failed`, `push-failed`, `no drafts`.

Maaike scans this in the morning before opening the dashboard.

---

## 2026-05-13 — git-pull-failed
- `git pull --rebase` aborted: unstaged changes in working tree (`.claude/launch.json`, `src/content/field-notes/three-defenses-against-confabulation.md`, `src/content/seeds/guildford-talk-ideas.md`).
- No drafts enriched. Resolve working tree state and re-run manually.
- log generated by nightly-inbox-enrichment task

## 2026-05-16
- enriched: just-a-moment (1 total) — DeepLearning.AI course on generative UI; renamed from blocked-page title not done (kept filename per skill); 7 new topics + 7 new tag stubs + 6 associations added
- skipped: google-search (webfetch-failed — consent redirect for Octavia E. Butler knowledge panel; not an article), youtube (webfetch-failed — YouTube returns only title via WebFetch, no description/transcript for TAO; per memory, Chrome MCP is the path but unattended run can't use it)
- note: existing weblink `linkedin.md` points to the same DeepLearning.AI course (via lnkd.in shortener) and has `triples: []`. Possible dedup candidate for Maaike's review.

## 2026-09-20
- enriched: waterr-delegate-meetings-to-ai-agents-creator-stud (1 total) — Waterr "Monologue" off-band reasoning sidecar for real-time voice; 9 new topics + 7 associations + 6 new tag stubs; title corrected from the site's generic marketing title to the actual research page title; kept `draft: true`
- skipped: just-a-moment (webfetch-failed — buff.ly resolves to uxdesign.cc "The register shift" on Medium, which returns HTTP 403 to WebFetch), www-mckinsey-com (webfetch-failed — 60s timeout on two attempts)
- note: first run since 2026-05-16. Only 3 un-enriched drafts were waiting, not a backlog.
- note: `www-mckinsey-com-1.md` is `draft: false` with `triples: []` — published but never enriched, so it is invisible to the graph and has no Mycelium. Outside this task's draft-only gate. Needs a manual `/auto-tag`.
- note: tags kept to 10. The two instance-specific topics (`monologue-waterr`, `audio-multichallenge`) were left as graph topics only, not promoted to tags, since they would not plausibly tag future posts.
- log generated by nightly-inbox-enrichment task
- ⚠ concurrent writes: this run fired late (11:43, not 05:00) and `tools/admin/server.py` (dashboard, PID 4952) was live. Nine draft weblinks (access-denied, checking-your-browser-recaptcha ×3, google-search, instagram-1, linkedin, linkedin-1, youtube-1) were being enriched in the dashboard *during* this run. Those edits were deliberately left uncommitted for Maaike to review. Only this task's own files were committed.
- ⚠ dual-write gap: those dashboard-enriched drafts have `themes:` and `triples:` in frontmatter but **no matching entries in `src/data/triples.json` or `themes.json`**. Per CLAUDE.md the dual-write is required, so as it stands they will render a Mycelium section but stay invisible to `/graph`, `/topics` and `/themes`. Worth checking whether the dashboard's Enrich path writes the JSON files at all.
